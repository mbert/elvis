"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
" TAKE A GUESS AT THE BUFFER'S TYPE (unless already known)
if readeol == "text" && security != "restricted"
then let! readeol=fileeol(filename)
